In terminal do the following:

In the chatzilla main folder type: 
	"bower install; npm install; python -m SimpleHTTPServer 8888"

Then open up another terminal and go to the server folder and type:
	"npm install; node chatserver.js"

Now open up a browser and type "localhos:8888"

VOILA